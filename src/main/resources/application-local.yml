spring:
  datasource:
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/profileDb}
    driverClassName: org.postgresql.Driver
    password: ${SPRING_DATASOURCE_PASSWORD:admin12345}
  jpa:
    hibernate:
      ddl-auto: none
  liquibase:
   changeLog: classpath:/db/changelog/db.changelog-master.xml
  data:
    elasticsearch:
      cluster-nodes: localhost:9200
  sleuth:
    sampler:
      probability: 1.0
logging:
  level:
    org:
      keycloak: DEBUG
#logging:
#  file:
#    name: /Users/macvebs/workspace/logging/post-service/logs/app.log
#  level:
#    root: info
#    liquibase: info
#    org.springframework.*: error
#logbook:
#  format:
#    style: http
#  filter:
#    default:
#      type: replace
#      value: "REDACTED"
#    multipart:
#      type: obfuscate
graylog:
  host: http://localhost:12201/gelf
keycloak:
  enabled: true
  auth-server-url: ${KEYCLOAK_URL:http://localhost:8080}
  credentials:
    secret: ${KEYCLOAK_CLIENT_SECRET:p4xJMVz0AbAZ44t4w5XE3B4HNO2dD4CH}
  ssl-required: external
  realm: ${KEYCLOAK_REALM:techbee}
  resource: personalisation_client
  bearer-only: true
  use-resource-role-mappings: false
aws:
  s3:
    bucket: techbeeimagebucket
  secretKey: wNSxzxaUhXVvr7rsdZbWWjBHjb8DzQ4a61R5Ttm9
  accessKey: AKIA5HGNSUXAVMNY5S4C
  region: us-east-1
